import { account } from './create';
import { aleph_create, aleph_get } from './create';

aleph_create(account, "fcb", {"rafinia" : "ad"} );
aleph_create(account, "psg", {"kimpembe" : "dc", "verrati" : "mc"} );

//const res = aleph_get(account.address);
//console.log(res);